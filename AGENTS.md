# AGENTS.md

## Cursor Cloud specific instructions

This repository (`studyguide-and-pratciec-questions`) is currently an empty project with only a `README.md`. There are no services, dependencies, build steps, or tests to run.

- **No package manager or dependencies** are configured yet. The update script is a no-op.
- **No lint, test, or build commands** exist. When code is added, update this section with the relevant commands.
- **No services** need to be started for development.

When the project gains actual code and dependencies, update the VM update script (via `SetupVmEnvironment`) and this file accordingly.
